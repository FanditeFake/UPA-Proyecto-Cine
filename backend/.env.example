# ── Servidor ──────────────────────────────
PORT=4000

# ── Base de datos (MySQL en Docker) ───────
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cinemax_db
DB_USER=cinemax_user
DB_PASSWORD=cinemax_pass

# ── JWT (autenticación) ───────────────────
JWT_SECRET=cambia_esto_por_una_cadena_larga_y_segura
JWT_EXPIRES_IN=8h
